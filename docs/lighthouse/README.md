# Lighthouse reports

Run `npm run lighthouse` to refresh the HTML and JSON reports for the deployed frontend.

The audit uses Lighthouse mobile defaults and writes:

- `sealara.report.html` — shareable visual report;
- `sealara.report.json` — machine-readable metrics.
