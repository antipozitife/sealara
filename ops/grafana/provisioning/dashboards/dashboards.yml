apiVersion: 1

providers:
  - name: "Sealara"
    orgId: 1
    folder: "Sealara"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
